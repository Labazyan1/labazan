name: Deploy to Beget (static v2)

# v2 — плоская статика. Ручной запуск. Заливка ./site/ по FTPS.
# Пред-шаг стирает remote sync-state → чисто добавляющая заливка.
on:
  workflow_dispatch:

concurrency:
  group: deploy-beget
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Lint PHP (форм-эндпоинт не должен уехать с синтакс-ошибкой)
        run: |
          for f in site/api/*.php; do echo "lint $f"; php -l "$f"; done

      - name: Clear remote sync-state (additive deploy)
        env:
          S: ${{ secrets.FTP_SERVER }}
          U: ${{ secrets.FTP_USERNAME }}
          P: ${{ secrets.FTP_PASSWORD }}
          D: ${{ secrets.FTP_SERVER_DIR }}
        run: |
          sudo apt-get update && sudo apt-get install -y lftp
          lftp -c "set ftp:ssl-force true; set ftp:ssl-protect-data true; set ssl:verify-certificate no; open -u \"$U\",\"$P\" \"$S\"; rm -f \"$D/.ftp-deploy-sync-state.json\"" || true

      - name: Deploy static site to Beget via FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftps
          port: 21
          local-dir: ./site/
          server-dir: ${{ secrets.FTP_SERVER_DIR }}
          dangerous-clean-slate: false
          exclude: |
            **/lead-config.php
            **/.git*
            **/.ftp-deploy-sync-state.json
