<?php
// ОБРАЗЕЦ конфига движка автопроверки сайта (check-html.php / check-speed.php).
// На сервере скопировать в audit-config.php и заполнить. Настоящий audit-config.php
// в .gitignore и закрыт в .htaccess (FilesMatch). В репозиторий кладём только *.sample.php.
//
// Секреты приёма ЛИД-формы (почта, n8n, Telegram) живут ОТДЕЛЬНО в lead-config.php —
// не смешиваем: разные секреты отзываются независимо.

// --- Замер скорости через Google PageSpeed Insights ---
// Бесплатный ключ: https://console.cloud.google.com → APIs & Services → Credentials,
// включить "PageSpeed Insights API". Пусто = замер скорости просто не запускается
// (основной разбор работает без него).
$PSI_API_KEY = '';

// --- Дневные потолки (глобальные, не по IP; сброс по дате UTC) ---
// Защита от превращения аудита в сканер чужих сайтов и от выгорания квоты PSI.
// 0 = потолок выключен. Оставьте пусто/закомментировано — возьмутся дефолты из эндпоинтов
// (html 500/сут, PSI 200/сут). Раскомментируйте, чтобы переопределить.
// $HTML_DAILY_MAX = 500;
// $PSI_DAILY_MAX  = 200;
